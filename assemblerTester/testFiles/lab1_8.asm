	LJMP START
	ORG	100H

START:
    SETB RS1
	CLR RS0
    MOV A, #7
    MOV R0, #1FH
	SJMP LOOP

LOOP:
    MOV @R0, A
    DEC R0
    DEC A

	JZ STOP
    SJMP LOOP
	
STOP:
    LCALL LCD_CLR
	MOV 0E0H, 1FH
	LCALL WRITE_HEX

	SJMP $
	NOP
